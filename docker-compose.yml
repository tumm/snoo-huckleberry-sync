services:
  snoo-sync:
    image: ghcr.io/tumm/snoo-huckleberry-sync:latest
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/dedupe.sqlite
    volumes:
      - snoo_data:/data

volumes:
  snoo_data:
