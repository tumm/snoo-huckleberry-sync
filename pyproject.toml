[project]
name = "snoo-huckleberry-sync"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-snoo",
    "huckleberry-api",
    "aiohttp",
    "python-dotenv",
]

[tool.uv]
package = false
